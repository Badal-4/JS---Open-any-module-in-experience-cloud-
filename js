openActivities : function(component,event,helper)
  {
    var url = window.location.origin + '/community/s/institution-activities';
    // open the tab in new window
    window.open(url, '_self').focus();
  }
